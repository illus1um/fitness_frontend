import 'package:flutter/material.dart';

class DrinkingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Water Tracker")),
      body: Center(child: Text("💧 Здесь будет трекинг воды")),
    );
  }
}
